# DeepRL_PolicyGradient
The algorithm is a solution for Openai's CartPole environment. The algorithm uses a Policy Gradient to train a neural network to predict the best action in a particular state.
